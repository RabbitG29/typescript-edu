let str: string = 'hello'
let num: number = 10

let arr: number[] = [1,2,3]

// TS 튜플(index에 따른 type 정의)
let address: [string, number] = ['gangnam', 2]

// TS 객체
let obj: object = {}
let person: { name: string, age: number } = {
    name: 'capt',
    age: 100
}